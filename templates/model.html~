<!doctype html>
<title>Model</title>
{% if udirlist %}
  {{ udirlist }}
{% else %}
  <h1>Hello 2nd World!</h1>
{% endif %}

<script>
    alert("top of model.html");
    window.addEventListener( "keydown", doKeyDown, false );
    alert("btm of model.html");
    
    function doKeyDown(e){
        //alert("keyCode: ["+e.keyCode+"]");
        if (e.keyCode == 39) {    // left arrow
            window.location = "/model/1";
        }
        else if (e.keyCode == 37) {    // right arrow
            window.location = "/model/0";
        }
        else if (e.keyCode == 13) {    // Enter
            window.location = "/mono/";
        }
    }  
</script>
  
